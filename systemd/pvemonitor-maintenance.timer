[Unit]
Description=PVEmonitor daily maintenance timer

[Timer]
# Run daily at 3:07 AM (off-peak, avoids :00/:30 stampede)
OnCalendar=*-*-* 03:07:00
AccuracySec=1min
Persistent=true

[Install]
WantedBy=timers.target
